/** Rename the app here; every surface (nav, manifest, tab title, backup filename) reads from this. */
export const APP_NAME = "Eyesic";

/** Lowercased, punctuation-free form of the name, for filenames. */
export const APP_SLUG = APP_NAME.toLowerCase().replace(/[^a-z0-9]+/g, "-");
export const APP_DESCRIPTION = "Pemutar musik pribadi bertenaga YouTube. Gratis, tanpa server.";
export const APP_THEME_COLOR = "#09090b";
